# Fine-grained GitHub PAT used only by scripts/update-data.ts
# and the Update data GitHub Action. Never sent to the browser.
GH_TOKEN=

# Optional. Override the GitHub GraphQL/REST host (default api.github.com).
# GITHUB_API_URL=https://api.github.com
# GITHUB_GRAPHQL_URL=https://api.github.com/graphql
